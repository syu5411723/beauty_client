import styled from "styled-components"

const Wrapper = styled.div`
    border:1px solid rgba(0,0,0,0.3);
    width:90%;
    padding: 20px 10px;
`
const Text = styled.div`
    
`

export const ContetFrame = () => {
    return (
        <Wrapper>
            
        </Wrapper>
    )
}
